#pragma once
#include <tokenize/types.h>
#include <tokenize/Token.h>
#include <tokenize/SourceFile.h>

namespace tokenize {
    class Tokenizer {
        public:
            Tokenizer();
            ~Tokenizer();
        
        protected:
    };
};